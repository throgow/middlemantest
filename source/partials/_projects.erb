<section class="gray">
    <h1 class="center">Selected work</h1>
    <div class="row">
      <% data.projects.projects.each do |project| %>
        <div class="col-md-4 card center">
          <h2>
            <%= project.title %>
          </h2>
          <div>
            <%= link_to 'View project', project.path, relative: true, :class=>'button'  %>
          </div>
        </div>
      <% end %>
    </div>
</section>
